# Daily Log — 2026-05-28

## End of Day Summary
**Date:** 2026-05-28
**What got done:**
- Built and started a runnable execution checklist in `BUILD-CHECKLIST.md`.
- Verified local stack workflow: started Postgres (when Docker engine was available), ran migrations, started admin server, and validated health endpoint.
- Ran a full dashboard-created audit end-to-end (test audit `#25`) from queued/running to complete.
- Validated delete-audit cleanup path via dashboard action; confirmed audit and report_artifact DB rows were removed and audit-specific files were cleaned.
- Performed report QA on latest outputs and identified quality issues (duplicate headings, empty day block, awkward phrasing, duplicate artifact cards).
- Implemented system-level tuning fixes in report/marp/admin rendering code and passed `npm run typecheck`.

**Key decisions made:**
- Defer SMTP/email testing for now.
- Prioritize report quality testing and output tuning before resuming email workflow.

**Open loops outstanding:**
- Docker Desktop engine became unstable (API 500 on `dockerDesktopLinuxEngine`), blocking DB-backed runtime verification.
- Re-run live verification of tuned report output (no duplicate headings, no empty day block, no duplicate artifact cards) after system restart.
- Configure and validate SMTP delivery flow after report-output QA is finalized.

**Tomorrow's first task:**
After restart, bring Docker/Postgres back up and run one full live regenerate + dashboard verification pass on the tuned report output.

## Post-restart verification (later same day)
**What got done:**
- Postgres healthy; admin restarted on `http://127.0.0.1:5059` with latest code.
- Regenerated audit `#24` via dashboard (`POST /audits/24/regenerate`).
- Confirmed compiled `reports/audit-24.md` has 9 unique `##` section headings (no consecutive duplicates).
- Confirmed admin detail shows one dark PDF + one light PDF in Test reports (no duplicate artifact cards).
- Found and fixed Day 7 drop in Marp: bullet text `from Day 2.` falsely matched day-header regex; `sevenDayPlanSlide.ts` now only matches line-start day headers. Re-regenerated; `audit-24-dark.md` includes Day 5, 6, and 7.

**Open loops:**
- SMTP/email delivery still deferred.
- Beta pricing and premium modules unchanged.

## Premium Marp report redesign
**What got done:**
- Reworked Competitive Instagram Action Guide slides (cover, jump map, scoreboard rows, score ranges, competitor 3+3 board, market comparison, mechanics, local checklist, richer 7-day plan, tear-off sheet, toolkit, grouped hashtags, ChatGPT prompts, checkpoints, closing).
- Wired `marpGenerator.ts` to new slide order; removed redundant `actionMovesSlide` and `doThisNextSlide` from deck.
- Added layout CSS to `themes/botlogix-dark.css` and `themes/botlogix-light.css` (overflow-safe competitor slots, profile preview, etc.).
- `npm run typecheck` passes; `npm run deck:botlogix` exports audit #24 dark/light PDF (~19 slides).

**Data limitations (audit #24):**
- Only 1 local competitor and 0 reference models in DB — deck shows honest empty slots, not invented accounts.
- Reference competitor pipeline still needs stronger discovery to fill Canadian reference column.

## Competitor discovery pipeline (same day)
**What got done:**
- Expanded `competitorDiscovery.ts`: category synonym queries, `site:instagram.com`, nearby-city queries, IG place-search fallback, per-market reference discovery (3 handles × up to 5 ON markets for marketing), mixed insert pool 5+5 before scrape.
- Fixed `competitorScrape.ts` reclassification: `local_intel` rows no longer nulled to `reference_model` with `geographic_market: null` (which dropped them from the report).
- Broadened `isAllowedReferenceMarketLabel` for Canadian `City, ON` labels + Toronto/Hamilton; `referenceMarketsForAudit` adds larger ON markets for marketing categories.
- `npm run typecheck` passes.

**To re-test on existing audits:** orchestrator skips discovery when competitor rows exist — delete audit or clear competitors, then run a fresh audit (not regenerate-only).

## 30-day push + Apify cost controls (continued)
**What got done:**
- Wired profile gate before competitor DB insert (`competitorDiscovery.ts` + `competitorProfileGate.ts`).
- Post-scrape profile gate short-circuit + Apify budget cap in `competitorScrape.ts` (`apifyBudget.ts`, default 48 runs/audit).
- Service-area geo hints (`citiesFromServiceArea` in `geoMarketExpansion.ts`) for Halton/GTA/etc.
- Post-scrape backfill loop (`competitorBackfill.ts` → `ensurePresentableLocalCompetitors` in orchestrator).
- New Marp slides: `thirtyDayPushSlide.ts` (4-week calendar + monthly ChatGPT review prompt) + `thirtyDayReviewSlide` (day-30 return CTA).
- Updated `closingSlide.ts` and `.env.example` for budget/gate env vars.
- `npm run typecheck` passes.

**Next:** Fresh audit (not regenerate on #24) to exercise discovery + gate + backfill; then `npm run deck:botlogix` or dashboard regenerate for PDF.

## Phase 1 — 30-Day Action Workbook (Marp)
**What got done:**
- Replaced client Marp flow with action-first **30-Day Instagram Action Workbook** order (cover → start here → baseline → five moves → sprint → week 1 → local unlock → market → competitors → hashtags/toolkit/prompts → checkpoints → day 30 → close).
- Removed audit-first slides (scoreboard, ranges, leaderboard, pattern dashboards) from main client deck.
- New `workbookSlides.ts` + impact/effort matrix in `fiveMovesWorkbookSlides`; light theme default; workbook CSS in `botlogix-light.css`.
- Admin pipeline builds light workbook PDF first; regenerated `reports/marp/botlogix-audit-26-light.pdf` (~19 slides).

**Next:** User review light PDF vs NotebookLM reference; Phase 2 HTML/PDF renderer when structure is approved.
