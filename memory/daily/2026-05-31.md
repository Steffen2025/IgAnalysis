# Daily Log — 2026-05-31

## Focus
IG intelligence pipeline: make competitor discovery rock-solid, productionize it,
and turn the output into a premium client deliverable.

## What got done

### Competitor discovery rebuild (was finding only 1 in thin niches)
- **Phase 1** — engagement-based hashtag TOP-posts discovery (primary lane).
  Found + fixed a live bug: bare `hashtags` input returns "no_items"; the actor
  needs `/explore/tags/<tag>/` page URLs via `directUrls`.
- **Phase 2** — `relatedProfiles` expansion anchored on on-topic accounts.
- **Phase 3** — soft follower band + engagement `successScore` as primary ranker;
  in-band → reference model, below-band → local peer, above-band → reject.
- **Phase 4** — Instagram-native user search (`searchType: "user"`) as a 4th lane.
  Confirmed: NO new Apify actor needed.
- Validated live across 3 category kinds: garage doors (service), mortgage
  (professional), BoxBuddy (app) — 6–8 real same-category accounts each.

### Report alignment + confidence
- Report selection now TRUSTS live-discovery rows (sources
  `reference_search`/`hashtag_discovery`) instead of re-deriving relevance with a
  stricter gate; closed an off-category leak and a legacy Canadian-market filter
  that was dropping valid references.
- Competitor-section confidence is now data-quality-driven (reference models
  present, post coverage), not a flat 80.

### Productionization
- `generateGoldMaster(auditId, { liveDiscovery })`; CLI `--live` on `generate`
  and `analyze`. Legacy audit orchestrator left untouched (no deck-path regression).

### Data correctness
- English-only gate at discovery AND render — purged the Chinese `@hkagencysecret`.
- Fixed band classification so above-band giants (e.g. 143k `@askaviarya`) are
  rejected, not mislabeled as local peers.

### Client deliverable — "Instagram Growth Intelligence Report"
- Blueprint in `docs/report-blueprint.md`.
- 20-section premium field guide (teaching layer on every number, growth-diagnosis
  funnel, profile blueprint, Days 1–30 roadmap, 30-day content calendar, toolkit).
- `deliverableMarkdown.ts` → `report.md`; `deliverableHtml.ts` → branded
  self-contained `report.html` (hero score, page breaks, embedded logo);
  `htmlToPdf.ts` → `report.pdf` (headless Edge/Chrome, no new dep).
- `gold-master.md` stays the technical/QA view.

### Tests + repo
- `npm test` = typecheck + 28 no-DB fixture tests (all green).
- Pushed to GitHub `Steffen2025/IgAnalysis` (origin/master); added the remote.

## Decisions
See `memory/projects/botlogix-ig-analysis.md` Decisions Log (2026-05-30/31 entries)
and AGENTS.md → LEARNED.

## Carry-forward / open loops
- Deliverable design-system polish (light-gray cards, softer dividers).
- Drop exact BotLogix wordmark at repo-root `BotLogix Logo.png` (could not save the
  chat-attached image bytes from here).
- Optional: pull each competitor's real top format/hook into the "borrow" line.
- Resume: SMTP delivery; 5-accounts-per-unit 30-day tracking.

## End of Day Summary
Competitor discovery is fixed and productionized; data is English-only and
correctly classified; the client deliverable is a polished 20-section field guide
in md + branded HTML + PDF. All committed and pushed. Memory system updated.
